= form_for @served_role do |f|
  - if @served_role.errors.any?
    #error_explanation
      %h2= "#{pluralize(@served_role.errors.count, "error")} prohibited this served_role from being saved:"
      %ul
        - @served_role.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
